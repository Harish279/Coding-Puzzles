#include<iostream>
using namespace std;

int main()
{
    int n,x=0;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    int leader;
    for(int i=0;i<n-1;i++)
    {
        leader=a[i];
        int flag=0;
        for(int j=i+1;j<n;j++)
        {
            if(leader>=a[j])
            {
                flag=1;
            }
            else
            {
                flag=0;
                break;
            }
            
        }
        if(flag==1)
            cout<<leader<<" ";
    }
    cout<<a[n-1];
return 0;
}
